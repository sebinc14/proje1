name: proje
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.13.1

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.0
  cached_network_image: ^3.3.1
  carousel_slider: ^5.0.0
  cupertino_icons: ^1.0.8
  
  # Serenay E-Commerce Widgets Paketi (Doğrudan ana daldan çekiyoruz)
  serenay_ecommerce_widgets:
    git:
      url: https://github.com/serenayyazilim/serenay-ecommerce-widgets.git
  image_picker: ^1.2.3
  firebase_storage: ^12.4.10
  intl: ^0.20.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true